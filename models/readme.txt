This is the model folder
It usually contains models that reflect the DB Table structure

At leaf we use MongoDB as a way of storing our data.
The Mongoose Package is very helpful in defining, manipulating, validating, sanitizing data going in  respective tables